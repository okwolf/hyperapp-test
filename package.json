{
  "name": "hyperapp-test",
  "version": "0.1.0",
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "hyperapp": "^0.12.1",
    "jest": "^21.1.0"
  },
  "peerDependencies": {
    "hyperapp": "^0.12.1"
  },
  "scripts": {
    "test": "jest --coverage --no-cache"
  },
  "babel": {
    "presets": "es2015"
  }
}
